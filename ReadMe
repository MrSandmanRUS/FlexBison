1. Стартовый состав папки FlexBison:

    1.1. Файл example.l - определения лексики языка ПЛ-1 (вход для утилиты flex).
    1.2. Файл example.y - определения синтаксиса языка ПЛ-1 (вход для утилиты bison).
    1.3. Файл vxod.pli  - тестовое demo-задание для сгенерированного компилятора PL_I.
    1.4. Файл ReadMe    - данный файл.
    1.5  RunScript      - скрипт запуска сеанса компиляции demo-залания vxod.pli компилятором PL_I.
    1.6  Makefile       - make-скрипт как для генерации компилятора PL_I (формат вызова - make),
                          так и для приведения папки FlexBison в исходный стартовый состав (формат вызова - make clear).

2. Последовательность действий по генерации и тестированию компилятора PL_I:

    2.1. Для генерации компилятора PL_I необходимо:
	    - войти в папку FlexBison (сделать ее текущей);
	    - выполнить директиву make.

    2.2. Для тестирования компилятора PL_I необходимо:
	    - войти в папку FlexBison (сделать ее текущей);
	    - выполнить скрипт RunScript.

3. Последовательность действий по приведению папки FlexBison в исходный стартовый состав:
	    - войти в папку FlexBison (сделать ее текущей);
	    - выполнить директиву make clear..

